[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "unicats"
version = "0.1.0"
dependencies = [
    "torch",
    "hydra-core",
]

[tool.setuptools.packages]
find = {}